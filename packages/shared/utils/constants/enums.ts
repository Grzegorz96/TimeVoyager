export enum UserStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    PENDING = "pending",
    BANNED = "banned",
}

export enum UserType {
    LOCAL = "local",
    GOOGLE = "google",
    DISCORD = "discord",
}
